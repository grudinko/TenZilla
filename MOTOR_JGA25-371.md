# Двигатель JGA25-371 в TenZilla

Краткая справка по мотор-редуктору **JGA25-371** и настройке энкодера.

## Параметры

| Параметр | Значение |
|----------|----------|
| Напряжение | 6–24 V DC (номинал 12 V) |
| Ток холостого хода | ~46 mA |
| Ток стопа | ~1 A |
| Скорость | 25–460+ RPM (зависит от варианта редуктора) |
| Энкодер | Hall (или оптический), **12 имп./об** (на валу редуктора) |
| Выход энкодера | 2 канала (A, B) — квадратура (Gray code) |
| Разъём | 6 pin: Motor +, Motor −, Hall VCC, Hall GND, Hall A, Hall B |

## Подключение к TenZilla

### Мотор (через реле)

- **Motor +** и **Motor −** — через драйвер/реле (RELAY_PIN_1, RELAY_PIN_2) к питанию 12 V.
- Не подавайте 12 V на GPIO. Используйте реле или MOSFET.

### Энкодер (в ESP32)

- **Hall VCC** → 3.3 V  
- **Hall GND** → GND  
- **Hall A** → GPIO 4 (`ENCODER_PIN_A`)  
- **Hall B** → GPIO 5 (`ENCODER_PIN_B`)  

## Режимы энкодера в `TenZillaPins.h`

У JGA25-371 энкодер **квадратурный** (A и B). Рекомендуется:

1. **Сначала попробовать квадратуру** (оба канала):
   - `ENCODER_PULSE_DIR` = **0**
   - `ENCODER_PULSE_ONLY` = **0**
   - `ENCODER_HALL` = **1**

2. Если **B не меняется** (отключение B ничего не меняет, счёт только в одну сторону):
   - `ENCODER_PULSE_ONLY` = **1** (считаем только A, B не используем).

3. **Направление**:
   - `ENCODER_INVERT_A` / `ENCODER_INVERT_B` = 1 — если направление счёта неверное.

## Шаг «мм на импульс»

**12 имп./об** — низкое разрешение. Шаг в мм зависит от механики (ход винта, передача и т.п.):

- Измерьте, сколько мм перемещается за один оборот вала редуктора.
- **Шаг = (мм за оборот) / 12** (если считаем 1 имп. = 1 «счёт»).
- Задайте это значение в веб-интерфейсе: **Настройки → Энкодер → Шаг в мм на импульс**.

## Итог

- **Мотор** — через реле/драйвер, 12 V.  
- **Энкодер** — A→GPIO4, B→GPIO5, VCC→3.3V, GND→GND.  
- **Режим** — квадратура; при проблемах с B → `ENCODER_PULSE_ONLY 1`.  
- **Шаг** — подобрать по механике и сохранить в настройках.

Подробнее по опциям энкодера см. **ENCODER_HALL.md**.
