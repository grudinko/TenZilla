# Энкодер с датчиками Холла — настройка

После замены на Hall-энкодер перемещение (MOV.) может перестать считаться. В коде добавлена поддержка Hall-режима.

**Двигатель JGA25-371:** см. **MOTOR_JGA25-371.md** (подключение, 12 имп./об, квадратура).

## 1. Включённый по умолчанию Hall-режим

В `TenZillaPins.h` задано:
- **`ENCODER_HALL 1`** — увеличенный debounce (100 µs), подходит для датчиков Холла.

Пересоберите и прошейте. Если счёт пошёл — больше ничего менять не нужно.

## 2. Если всё ещё не считается

### Режим «только импульсы» (A, B не используется)

Если **B не меняется** (отключение B ничего не меняет, счёт только в одну сторону):
- Установите **`ENCODER_PULSE_ONLY 1`**, **`ENCODER_PULSE_DIR 0`**.
- Считаем каждый фронт по A; направление не определяется.

### Режим «импульс + направление»

Если модуль даёт **импульсы (A) и отдельно направление (B)**:
- **`ENCODER_PULSE_DIR 1`**, **`ENCODER_PULSE_ONLY 0`**.
- A → GPIO 4, B → GPIO 5. B = HIGH обычно «вперёд», LOW — «назад».

### Считает только в плюс (или только в минус)

Если импульсы есть, но счёт идёт только в одну сторону:
- Установите **`ENCODER_PULSE_DIR_INVERT 1`**. Тогда B LOW → +1, B HIGH → −1.
- Убедитесь, что **B подключён** к GPIO 5 (иначе подтяжка даёт только «одно» направление).

### Инверсия каналов

Если направление счёта обратное:
- **`ENCODER_INVERT_A 1`** или **`ENCODER_INVERT_B 1`** в `TenZillaPins.h`.

### Шаг энкодера (мм/импульс)

У Hall-энкодеров часто **меньше импульсов на оборот**. Проверьте **«Шаг в мм на импульс»** в веб-интерфейсе (Настройки → Энкодер). Увеличьте значение, если перемещение в мм слишком мало при том же движении.

## 3. Подключение

- **A** → GPIO 4, **B** → GPIO 5, **VCC** → 3.3 V, **GND** → GND.
- Выходы Hall обычно **open-drain**; внутренняя подтяжка (INPUT_PULLUP) используется в коде.

## 4. Сводка опций в `TenZillaPins.h`

| Опция | Значение | Описание |
|-------|----------|----------|
| `ENCODER_HALL` | 1 | Режим Hall (debounce 100 µs) |
| `ENCODER_PULSE_DIR` | 0 / 1 | 1 = импульс+направление (A/B) |
| `ENCODER_PULSE_ONLY` | 0 / 1 | 1 = только A, B не используется |
| `ENCODER_PULSE_DIR_INVERT` | 0 / 1 | 1 = B LOW → +1, B HIGH → −1 |
| `ENCODER_INVERT_A` | 0 / 1 | Инверсия канала A |
| `ENCODER_INVERT_B` | 0 / 1 | Инверсия канала B |
